enum layers {
  _DEFAULT = 0,
  _LOWER,
  _RAISE,
  _ADJUST,
  _MOUSE,
};

enum custum_keycodes {
  // hold: CTRL, tap: question
  HT_CTL_QUES = KEYBALL_SAFE_RANGE,
  TOGG_WIN_MAC,
  WM_CTL_ALT,
  LOWER,
  RAISE,
};

// tap dance keys
enum {
  TD_ESC_CAPS = 0,
  TD_YEN_PIPE,
  TD_HIHN_EQL,
  TD_LBRC_LBRC,
  TD_RBRC_RBRC,
  TD_BSLSH_UDSCR
};